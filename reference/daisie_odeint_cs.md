# Driver for the boost::odeint solver for the CS model

Driver for the boost::odeint solver for the CS model

## Value

Object of type \`state_type\`, which itself is \`vector_t\`\<double\>,
with the result of the integration depending on the runmod chosen.
