# CS iteration control

Sets or retrieves the max. number of iterations used by the odeint
solver.

## Usage

``` r
DAISIE_CS_max_steps(max_steps)
```

## Arguments

- max_steps:

  `max_steps`: sets max. iterations to `max_steps`.  

## Value

current max. iterations
